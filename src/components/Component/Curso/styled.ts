import styled from 'styled-components'
import { Color } from '../../../style'

export const EstiloCertificado = styled.div`
  h1 {
    margin-bottom: 8px;
    font-weight: 400;
    font-size: 25px;

    span {
      color: ${Color.laranja};
      font-size: 25px;
      font-weight: 700;
      font-family: 'Leckerli One', cursive;
    }
  }
`
